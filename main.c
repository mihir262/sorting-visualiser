#include <stdio.h>
#include <raylib.h>

#define WIDTH 900
#define HEIGHT 600

int main(){
    
    InitWindow(WIDTH, HEIGHT, "Sorting Visualiser");

    while(!WindowShouldClose()){
        BeginDrawing();

        EndDrawing();
    }
    
    return 0;
}

